<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"> 
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style/style.css"> <!-- Corrected path -->
   <title>Edit Quote</title>
</head>
<body>
    <h1>Edit Quote</h1>
    <%- include('partial/nav') %>
    <% if (message) { %>
        <p style="color: green;"><%= message %></p>
    <% } %>
    <form action="/editQuote" method="POST">
        <input type="hidden" name="quoteId" value="<%= quote.quoteId %>">

        <label for="quote">Quote:</label>
        <textarea id="quote" name="quote" required><%= quote.quote %></textarea>
        <br>

        <label for="authorId">Author ID:</label>
        <input type="number" id="authorId" name="authorId" value="<%= quote.authorId %>" required>
        <br>

        <label for="category">Category:</label>
        <input type="text" id="category" name="category" value="<%= quote.category %>" required>
        <br>

        <label for="likes">Likes:</label>
        <input type="number" id="likes" name="likes" value="<%= quote.likes %>" required>
        <br>

        <button type="submit" class="btn btn-success">Update</button>
    </form>
    <script src="/js/quote.js"></script>
</body>
</html>